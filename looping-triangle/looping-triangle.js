const triangle = () => {
    for (let hashtag="#"; hashtag.length<=7; hashtag+="#") {
        console.log(hashtag)
    }
}

console.log(triangle())

let number = 0;
while (number <= 12) {
  console.log(number);
  number = number + 2;
}

for (let hashtag="#"; hashtag<8; hashtag++) {
    console.log(hashtag)
    hashtag = hashtag+"#"
}